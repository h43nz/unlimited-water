services:
  unlimited-water:
    image: nginx:alpine-slim
    container_name: unlimited-water-web
    restart: always
    ports:
      - "8090:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
      - ./css:/usr/share/nginx/html/css:ro
      - ./js:/usr/share/nginx/html/js:ro
      - ./assets:/usr/share/nginx/html/assets:ro
    # User-defined networks if needed for Pangolin/Proxy
    # networks:
    #   - proxy_network 

# networks:
#   proxy_network:
#     external: true
